# psycopg2/bcrypt/playwright/markdown/PyMuPDF 등은 이미 로컬 환경에
# 설치돼 있는 기존 Streamlit 앱 의존성을 그대로 재사용한다 (재설치 안 함).
# 주의: 루트 requirements.txt 의 streamlit==1.56.0 은 Python 3.10+ 필요라
# 로컬 Python 3.9 환경과 안 맞음 -r 로 상속하지 않는다.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.0
